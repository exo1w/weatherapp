name: UI Service CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'weatherapp-microservices/ui/**'

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Login to DockerHub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build Docker Image
      run: |
        docker build -t dockerle12/weatherapp-ui:${{ github.sha }} weatherapp-microservices/ui/UI
        docker tag dockerle12/weatherapp-ui:${{ github.sha }} dockerle12/weatherapp-ui:latest

    - name: Push Docker Image
      run: |
        docker push dockerle12/weatherapp-ui:${{ github.sha }}
        docker push dockerle12/weatherapp-ui:latest

    - name: Setup Kubeconfig
      run: |
        mkdir -p $HOME/.kube
        echo "${{ secrets.KUBE_CONFIG }}" > $HOME/.kube/config

    - name: Update Deployment Image
      run: |
        kubectl set image deployment/weatherapp-ui \
        weatherapp-ui=dockerle12/weatherapp-ui:${{ github.sha }}

    - name: Verify Rollout
      run: |
        kubectl rollout status deployment/weatherapp-ui
