apiVersion: v1
kind: Service
metadata:
  name: weatherapp-auth
spec:
  selector:
    app.kubernetes.io/name: weatherapp-auth
  type: ClusterIP                      
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP