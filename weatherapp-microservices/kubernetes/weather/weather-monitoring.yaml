apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: weatherapp-weather-monitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: weatherapp-weather
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
